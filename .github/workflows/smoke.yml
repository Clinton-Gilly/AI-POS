name: Actions smoke test

# Diagnostic only. If this does not run, the problem is not any workflow's
# YAML — it is that Actions is unavailable for this repository (disabled in
# Settings → Actions, or blocked by billing / an org policy). Delete this file
# once CI is confirmed working.
on:
  workflow_dispatch:
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Actions is working in this repository."
